{
  "version": 2,
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "student-task-manager/client/dist",
  "routes": [
    {
      "src": "/[^.]+",
      "dest": "/index.html"
    }
  ]
}
